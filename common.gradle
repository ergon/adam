versioning {
    postProcessVersion = { version ->
        version.matches('^[^-]+(-alpha|-beta)?[^-]*$') ? version : "${version}-SNAPSHOT"
    }
    match = 'v*'
}

version = versioning.describeVersion()

